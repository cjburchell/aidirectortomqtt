# AI Memory

Durable AI development context for this repository lives here.

## Files

- `architecture.md` describes the stable system shape and package responsibilities.
- `tech-context.md` records stack, commands, runtime configuration, Docker, and CI notes.
- `active-context.md` records current repository notes and follow-up risks.
- `ProjectSetupForClaudeCodeContext.md` is the original project-agnostic setup prompt that guided this context structure.

Start at the root `AI_CONTEXT.md` and only open these files as needed for the task.
